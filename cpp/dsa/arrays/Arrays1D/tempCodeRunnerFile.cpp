int max = a[0];
    // int smax = a[0];
    // for(int i = 1; i<n; i++) {
    //     if (smax<=a[i] && a[i]<max) {
    //         smax = max;
    //     }
    //     if (max<=a[i]) {
    //         max = a[i];
    //     }  
    // }
    // cout<<"The maximum element is : "<<max<<endl;
    // cout<<"The second maximum element is : "<<smax<<endl;